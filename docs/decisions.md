# Decisions log

Choices made during the event that are not derivable from the code. Newest at the bottom. Add to this when you make a call the other person or a future agent would otherwise have to rediscover.

| When | Decision | Why |
|---|---|---|
| Sat hour 0 | Repo is a single Vite + React + TS package, no workspaces. Zustand, Tailwind v4, vitest, tsx, eslint flat config. | Speed. Boundaries are enforced by lint, not by package layout. |
| Sat hour 0 | React pinned to 19.2.x. | `@react-three/fiber` 9.x refuses React 19.3. |
| Sat hour 0 | Five contract edits to the original scaffold prompt: `Reading` has `sensorId` and `source`; `StructurePlan` with per-edge rates exists; brain takes an `Observation` bundle (readings + drone self-reports) not bare readings; a `Corruptor` interface exists; `Belief` has `suspectSensors`. | Fixed hardwired sensors are the trusted reference in the plan. Edge rates must be data. The brain cannot allocate drones it cannot locate. The corruption directory needed a contract. The UI needs to show distrusted sensors. |
| Sat hour 0 | Adjacency is derived from edges at load time. Plan JSON has `spaces` and `edges`, not `neighbors`. | One source of truth; no inconsistency between a neighbor list and an edge list. |
| Sat hour 0 | RNG streams are forked per subsystem: `rng.fork('world')`, `'corruption'`, `'brain'`. | Adding a draw in one subsystem must not shift the others, or seed-matched comparisons break. |
| Sat hour 0 | The world never lies. Every way a reading can be wrong, including sensor death from heat and drone comms loss, lives in `src/corruption`. The world owns physical drone death and fire physics. | One place to state the failure model for the writeup. The corruptor sees clean temps so it can key correlated failures off them without importing the world. |
| Sat hour 0 | `CorruptionConfig` is the chaos panel's API. Dean adds fields; Chase's panel is generated from a schema with a compile-time exhaustiveness check. | Neither person edits the other's directory when a knob is added. |
| Sat hour 0 | Brain also cannot import `src/corruption`, `src/eval`, `src/ui`, or `src/loop`, not just `src/world`. | Importing the injector would leak the failure pattern. |
| Sat hour 2 | Checkpoints are every 12 hours (hours 12/24/36/48/60), final at 72 (Tue noon ET, assumed from the Playbook). The pitch doc's 8-hour cadence is superseded. | Organizer confirmation relayed by Dean. |
| Sat hour 2 | Method freeze moves from hour 24 to hour 36 (CP3, Sunday midnight ET). | Keeps freeze at checkpoint 3 under the new cadence. Gives the estimator 12 more hours; still leaves 36 hours of post-freeze evaluation. |
| Sat hour 2 | After the freeze, new brain code (allocator, briefings) goes through `src/brain/commands.ts`, an unfrozen hook file, so frozen files change by one import and one line. | The freeze test diffs frozen files against the recorded hash. |
| Sat hour 2 | Kalman baseline uses the graph Laplacian from plan edges as its process model. | It must be competent, not a straw man, or the comparison is worthless. |
| Sat hour 2 | Estimator design: hypothesis sets over candidate burning sets, physics-predicted temps, drop the k largest residuals, keep every hypothesis within tolerance of the best. Confidence = 1 / kept hypotheses × consistency penalty. | Direct implementation of the brief's y = Hx + a sparse-corruption framing; produces honest ambiguity as a set rather than a point. |
| Sat hour 2 | Docs `05` to `09` are reserved for artifacts produced during the event: identifiability, freeze, what-surprised-us, writeup, generality. | The prompts reference them by name. |
| Sat hour 3 | Claude Code automation committed to the repo: ownership/contract/freeze guard (PreToolUse), eslint+tsc after every edit (PostToolUse), test gate on Stop with a 3-strike release, `/dean` `/chase` `/build` `/checkpoint` skills, a `verifier` subagent, GitHub Actions CI. Role from git user.name. | Two people and their agents share one repo; the boundaries in docs/04 are enforced by machinery, not memory. Dean chose these from a menu; the two-agent write-then-verify loop is his standard workflow. See docs/automation.md. |
| Sat hour 3 | Per-person branches `dean-branch` and `chase-branch`, merged to `main` at every checkpoint or whenever the other person needs the work. `main` stays green and is what `/checkpoint` tags. CI runs on every branch. | Dean's call. Keeps a half-finished prompt off `main` while still merging often enough that conflicts stay small. |
| Sat hour 5 | All corruption modes respect `onset`, including flashover and saturate (CP1 had treated those as always-on physics). | The evidence window is measured from onset; ungated modes made the reported numbers measure the wrong window. The contract comment says onset is when "failures may begin" — now true for every mode. |
| Sat hour 5 | Brain default k=1, not the prompt's 2, exposed as `BrainConfig.k` (additive contract change). | With one sensor per space (every current plan), k=2 lets the "no fire" hypothesis discard both witnesses of a small fire. k must stay below per-space sensor redundancy; raise it only on plans with doubled-up sensors. |
| Sat hour 5 | Hypothesis scoring: physics rollout from the estimate 3 ticks ago to now, NOT the prompt's 50/50 steady-state blend; only downward residuals droppable; ties break toward the hypothesis needing no liars. | Steady state over-predicts unreached equilibria so badly that "no fire" beat the true hypothesis for 10 straight ticks. Every corruption mode pushes readings down, so an upward contradiction is never excusable. All probed before deciding; repros in the brain test suite. |
| Sat hour 5 | Certainty is earned: confidence caps at 0.7 until the burning set has been uncontested, unchanged, AND well-fitting for 5 consecutive ticks; zero trusted readings floors confidence at 0.05. | Three verifier rounds showed confidence-1.00-while-wrong sneaking through hypothesis-set collapse, belief flip-flops, and single-tick fit dips. Tradeoff: some clean seeds take ~25+ ticks to show conf >= 0.9; tuning knobs are in src/brain/index.ts and are a pre-freeze human call. |
| Sat hour 5 | Heat generation in a burning space is a pull toward a flame temperature, `GEN_RATE * (FLAME_TEMP - temp)` with 0.25 and 900 C, not the constant `+40 C/tick` in Chase's CP1 prompt 1. Constants in `src/world/constants.ts`. | The verifier showed the constant source never spreads on a two-level plan or a 5x3 grid (neighbors dilute the heat) and raising it to where it does gives 2600 C peaks. The pull form plateaus near 800 C on any plan size and is still linear in T (diagonal `1 - GEN_RATE`, constant `GEN_RATE * FLAME_TEMP`), so Dean's linear model still matches. Rate chosen by sweep over demo-6, a two-level demo-6, 5x3 and 8x8 grids and a 5x3 two-level grid; 0.15 still stalled on the larger grids, 0.25 spreads on all of them. |
| Sat hour 6 | Tethers die at 900 C (`DRONE_DEATH_TETHER = FLAME_TEMP`), not the 500 C in Chase's CP1 prompt 2, and 'suppress' adds 0.1/tick water cooling toward ambient on top of the 0.3 generation multiplier. Free-flying drones die at 400 C as written. | At 500 C a tether could never enter a burning space (a burning space is past 500 one tick after ignition and a drone will not step into a lethal space). A tethered unit is on the standpipe; water-cooled survival below flame temperature is the physical story. Nothing in the current plans reaches 900, so in practice only the corruptor can silence a tether: tethers and fixed sensors are the durable references. The cooling term is a tuning choice, not a necessity: the 0.3 multiplier alone holds a burning space near 410-450 C for 20 ticks then creeps toward 700; with cooling one tether holds it near 300-360 C and stops all spread, two hold it near 100 C. Side effect for the allocator: 'suppress' on a warm unburned space is also a strong action. |
| Sat hour 5 | Dean added the live split view (`src/ui/split/`, new `store.ts` and `App.tsx`, fonts in `index.html`) with Chase's directory rules waived by Dean for this one change, and merged it to `main` early so `npm run dev` shows truth, ours, and Kalman side by side. `src/eval/viewerData.ts` is the shared shaper; `npm run viewer` exports the same view as a standalone HTML for filming. | The terminal hid the ambiguous set and made the CP2 brain look worse than it is. Chase: rebase `chase-branch` on `main`; mount the Three.js scene as another view in `App.tsx` and leave `src/ui/split/` alone. |
| Sat hour 5 | Fixed a scaffold bug: `loop.ts` read `process.argv` at module load, which crashed every browser page since commit one. | Found when localhost rendered black. Contract file; additive guard only. |
| Sat hour 5 | Pre-freeze intent: add `probability: Record<SpaceId, number>` to `Belief` (additive) so ambiguity is graded per space instead of a binary MAYBE. The split view already renders it when present. | Dean's question: five fresh sensors at 830° should read as near-certain, not coin-flip. The allocator needs P(burning) anyway. |
| Sat hour 10 | Graded belief landed (CP2 prompt 4, last estimator change before the freeze): `Belief.probability` per space = weighted fraction of kept hypotheses (weight `exp(-(score-best)/tolerance)`), times the 0.8 suspect penalty only for the space's own distrusted sensor; Kalman reports 0 or its scalar confidence. Forced candidates: a space whose TRUSTED reading has been above ignition (250 C) for 3 consecutive ticks goes into every candidate, gated by an assumed fuel budget (1/0.02 ticks, 1.5x faster for 'fuel' hazard, minus a 5-tick margin) counted from when the brain first believed the space burning. Also fixed: the impossible-rise/drop bounds now let a neighbor with no trusted reading be anywhere between ambient and flame temperature. New metrics: Brier, per-space FPR/FNR at P>=0.5. | Before, seed-42 freeze tick 50: ours [S1,S2] at 0.16 with S3..S6 one flat MAYBE while F4,F5,F6 read 800+ honestly. After: [S1,S2,S4,S5,S6] at 0.40, S3 MAYBE at P~0.4. Evidence (5 seeds, 60 ticks): false certainty ours 0% on all three modes both before and after; coverage 91% -> 93%; mean error 3.6/3.7/8.0 -> 4.6/4.8/10.2 C (freeze/blind/flashover); Brier ours 0.067/0.069/0.078 vs Kalman 0.123/0.181/0.117. Why the fuel gate: without it a burned-out space (still 750 C from its burning neighbors) is forced forever, and on a 120-tick run every space ends up forced into one hypothesis, i.e. false certainty after total burnout; with it, scoring takes over and a burned-out space becomes a MAYBE or is dropped. Why the bound fix: the world-consistent test streams showed an honest sensor flagged impossible-rise when its space ignited next to a space whose sensor had died (estimate 429 C vs true 512 C). Tried and reverted: rolling a hypothesis with each space burning only since the brain first believed it (fixes the parent fire dropping to MAYBE for ~3 ticks when a neighbor ignites, but removes the leakage signal that separates "just ignited" from "not burning", and a clean run then never earns confidence). That MAYBE window is a recorded limitation, not a bug. |
| Sat hour 11 | Split view v2 (CP2 prompt 5): geometry is computed once in `buildViewerData` (`src/eval/layout.ts`) and shipped as JSON in `ViewerData.layout`, so `src/ui/split/svg.ts` and the plain-JS `viewer.template.html` draw from identical positions instead of two copies of the algorithm. Demo beats live in the store as pure `beatConfig` data. `src/shared/structures.ts` on dean-branch registers only demo-6; Chase's version on main registers all three plans with the same exports. Recording mode is `?demo=1` (plus `?plan=`, `?beat=`, `?t=` for filming). | The exporter is the filming backup and must look identical to the live page; a duplicated layout algorithm would drift. At merge, take Chase's `structures.ts`; the split view, exporter and tests only use `PLAN_NAMES`, `loadPlan`, `isPlanName`. `store.ts` keeps Chase's names (`planName`, `setPlanName`, `sanitizeCorruption`, `sanitizeIgnition`, `trace`, `tick`) so his 3D scene mounts without changes; take Dean's store at merge, it is a superset. |
| Sun hour 12 | `computeMetrics(trace, { onset, confidenceThreshold? })` (CP3 prompt 1): every windowed metric, including estimation error, now counts ticks >= onset; `TickRecord` carries `stepMs` (performance.now around `brain.step` only) and `onset` (null for a clean run). `timeToRecovery` = ticks from onset to the first of five consecutive exact matches; `wrongDispatch` = fraction of ticks where the burning set names a space that is neither burning nor hedged as ambiguous. | The brief's four metrics need a defined window and a compute cost per tick. Windowing the error moves the CP2 evidence mean error from 4.6/4.8/10.2 to 4.9/5.1/10.9 C (freeze/blind/flashover) and Kalman's from 42.0/114.7/41.9 to 44.8/122.7/44.7; false certainty, coverage and Brier are unchanged. `stepMs` is wall-clock, so determinism checks compare traces with it stripped. |
