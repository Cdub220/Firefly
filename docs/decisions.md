# Decisions log

Choices made during the event that are not derivable from the code. Newest at the bottom. Add to this when you make a call the other person or a future agent would otherwise have to rediscover.

| When | Decision | Why |
|---|---|---|
| Sat hour 0 | Repo is a single Vite + React + TS package, no workspaces. Zustand, Tailwind v4, vitest, tsx, eslint flat config. | Speed. Boundaries are enforced by lint, not by package layout. |
| Sat hour 0 | React pinned to 19.2.x. | `@react-three/fiber` 9.x refuses React 19.3. |
| Sat hour 0 | Five contract edits to the original scaffold prompt: `Reading` has `sensorId` and `source`; `StructurePlan` with per-edge rates exists; brain takes an `Observation` bundle (readings + drone self-reports) not bare readings; a `Corruptor` interface exists; `Belief` has `suspectSensors`. | Fixed hardwired sensors are the trusted reference in the plan. Edge rates must be data. The brain cannot allocate drones it cannot locate. The corruption directory needed a contract. The UI needs to show distrusted sensors. |
| Sat hour 0 | Adjacency is derived from edges at load time. Plan JSON has `spaces` and `edges`, not `neighbors`. | One source of truth; no inconsistency between a neighbor list and an edge list. |
| Sat hour 0 | RNG streams are forked per subsystem: `rng.fork('world')`, `'corruption'`, `'brain'`. | Adding a draw in one subsystem must not shift the others, or seed-matched comparisons break. |
| Sat hour 0 | The world never lies. Every way a reading can be wrong, including sensor death from heat and drone comms loss, lives in `src/corruption`. The world owns physical drone death and fire physics. | One place to state the failure model for the writeup. The corruptor sees clean temps so it can key correlated failures off them without importing the world. |
| Sat hour 0 | `CorruptionConfig` is the chaos panel's API. Dean adds fields; Chase's panel is generated from a schema with a compile-time exhaustiveness check. | Neither person edits the other's directory when a knob is added. |
| Sat hour 0 | Brain also cannot import `src/corruption`, `src/eval`, `src/ui`, or `src/loop`, not just `src/world`. | Importing the injector would leak the failure pattern. |
| Sat hour 2 | Checkpoints are every 12 hours (hours 12/24/36/48/60), final at 72 (Tue noon ET, assumed from the Playbook). The pitch doc's 8-hour cadence is superseded. | Organizer confirmation relayed by Dean. |
| Sat hour 2 | Method freeze moves from hour 24 to hour 36 (CP3, Sunday midnight ET). | Keeps freeze at checkpoint 3 under the new cadence. Gives the estimator 12 more hours; still leaves 36 hours of post-freeze evaluation. |
| Sat hour 2 | After the freeze, new brain code (allocator, briefings) goes through `src/brain/commands.ts`, an unfrozen hook file, so frozen files change by one import and one line. | The freeze test diffs frozen files against the recorded hash. |
| Sat hour 2 | Kalman baseline uses the graph Laplacian from plan edges as its process model. | It must be competent, not a straw man, or the comparison is worthless. |
| Sat hour 2 | Estimator design: hypothesis sets over candidate burning sets, physics-predicted temps, drop the k largest residuals, keep every hypothesis within tolerance of the best. Confidence = 1 / kept hypotheses × consistency penalty. | Direct implementation of the brief's y = Hx + a sparse-corruption framing; produces honest ambiguity as a set rather than a point. |
| Sat hour 2 | Docs `05` to `09` are reserved for artifacts produced during the event: identifiability, freeze, what-surprised-us, writeup, generality. | The prompts reference them by name. |
| Sat hour 3 | Claude Code automation committed to the repo: ownership/contract/freeze guard (PreToolUse), eslint+tsc after every edit (PostToolUse), test gate on Stop with a 3-strike release, `/dean` `/chase` `/build` `/checkpoint` skills, a `verifier` subagent, GitHub Actions CI. Role from git user.name. | Two people and their agents share one repo; the boundaries in docs/04 are enforced by machinery, not memory. Dean chose these from a menu; the two-agent write-then-verify loop is his standard workflow. See docs/automation.md. |
| Sat hour 3 | Per-person branches `dean-branch` and `chase-branch`, merged to `main` at every checkpoint or whenever the other person needs the work. `main` stays green and is what `/checkpoint` tags. CI runs on every branch. | Dean's call. Keeps a half-finished prompt off `main` while still merging often enough that conflicts stay small. |
| Sat hour 5 | All corruption modes respect `onset`, including flashover and saturate (CP1 had treated those as always-on physics). | The evidence window is measured from onset; ungated modes made the reported numbers measure the wrong window. The contract comment says onset is when "failures may begin" — now true for every mode. |
| Sat hour 5 | Brain default k=1, not the prompt's 2, exposed as `BrainConfig.k` (additive contract change). | With one sensor per space (every current plan), k=2 lets the "no fire" hypothesis discard both witnesses of a small fire. k must stay below per-space sensor redundancy; raise it only on plans with doubled-up sensors. |
| Sat hour 5 | Hypothesis scoring: physics rollout from the estimate 3 ticks ago to now, NOT the prompt's 50/50 steady-state blend; only downward residuals droppable; ties break toward the hypothesis needing no liars. | Steady state over-predicts unreached equilibria so badly that "no fire" beat the true hypothesis for 10 straight ticks. Every corruption mode pushes readings down, so an upward contradiction is never excusable. All probed before deciding; repros in the brain test suite. |
| Sat hour 5 | Certainty is earned: confidence caps at 0.7 until the burning set has been uncontested, unchanged, AND well-fitting for 5 consecutive ticks; zero trusted readings floors confidence at 0.05. | Three verifier rounds showed confidence-1.00-while-wrong sneaking through hypothesis-set collapse, belief flip-flops, and single-tick fit dips. Tradeoff: some clean seeds take ~25+ ticks to show conf >= 0.9; tuning knobs are in src/brain/index.ts and are a pre-freeze human call. |
| Sat hour 5 | Heat generation in a burning space is a pull toward a flame temperature, `GEN_RATE * (FLAME_TEMP - temp)` with 0.25 and 900 C, not the constant `+40 C/tick` in Chase's CP1 prompt 1. Constants in `src/world/constants.ts`. | The verifier showed the constant source never spreads on a two-level plan or a 5x3 grid (neighbors dilute the heat) and raising it to where it does gives 2600 C peaks. The pull form plateaus near 800 C on any plan size and is still linear in T (diagonal `1 - GEN_RATE`, constant `GEN_RATE * FLAME_TEMP`), so Dean's linear model still matches. Rate chosen by sweep over demo-6, a two-level demo-6, 5x3 and 8x8 grids and a 5x3 two-level grid; 0.15 still stalled on the larger grids, 0.25 spreads on all of them. |
| Sat hour 6 | Tethers die at 900 C (`DRONE_DEATH_TETHER = FLAME_TEMP`), not the 500 C in Chase's CP1 prompt 2, and 'suppress' adds 0.1/tick water cooling toward ambient on top of the 0.3 generation multiplier. Free-flying drones die at 400 C as written. | At 500 C a tether could never enter a burning space (a burning space is past 500 one tick after ignition and a drone will not step into a lethal space). A tethered unit is on the standpipe; water-cooled survival below flame temperature is the physical story. Nothing in the current plans reaches 900, so in practice only the corruptor can silence a tether: tethers and fixed sensors are the durable references. The cooling term is a tuning choice, not a necessity: the 0.3 multiplier alone holds a burning space near 410-450 C for 20 ticks then creeps toward 700; with cooling one tether holds it near 300-360 C and stops all spread, two hold it near 100 C. Side effect for the allocator: 'suppress' on a warm unburned space is also a strong action. |
| Sat hour 8 | The tower-5x4 "stack effect" test asserts the shaft space ignites first on every level above the fire and that the top level burns at least 10 ticks sooner than on the same plan with no shaft (t=34-43 vs 56-59), not the prompt's "shaft space on level 5 before any non-shaft space on level 3". | A shaft hop costs ~5 ticks (the source must ignite and climb before the next space reaches 250 C) and a door hop ~3, so three shaft hops never beat one door hop; a sweep over shaft position, ignition space and passage rate never gets closer than 4 ticks. Edges are symmetric, so fire also descends the shaft; a true stack effect needs directional rates, deferred. |
| Sun hour 12 | The 3D truth scene (`src/ui/scene/`) is a second view beside Dean's split view, switched by a top bar that also holds the structure select; Chase's CP2 prompt 2 said "replace the table layout" but that layout had already become the CP1 demo. The scene colors by a `SceneFrame` built from truth today and from a belief at CP4. Corruption targets are sanitized against the current plan on load, run and plan change (unknown targets dropped, fallback to the ignition space). URL params `view`, `plan`, `t` preselect for screenshots. | Keeps the CP1 demo intact and lets both views share one store. A target saved to localStorage on one plan was otherwise restored on another and silently turned corruption off. |
| Sat hour 5 | Dean added the live split view (`src/ui/split/`, new `store.ts` and `App.tsx`, fonts in `index.html`) with Chase's directory rules waived by Dean for this one change, and merged it to `main` early so `npm run dev` shows truth, ours, and Kalman side by side. `src/eval/viewerData.ts` is the shared shaper; `npm run viewer` exports the same view as a standalone HTML for filming. | The terminal hid the ambiguous set and made the CP2 brain look worse than it is. Chase: rebase `chase-branch` on `main`; mount the Three.js scene as another view in `App.tsx` and leave `src/ui/split/` alone. |
| Sat hour 5 | Fixed a scaffold bug: `loop.ts` read `process.argv` at module load, which crashed every browser page since commit one. | Found when localhost rendered black. Contract file; additive guard only. |
| Sat hour 5 | Pre-freeze intent: add `probability: Record<SpaceId, number>` to `Belief` (additive) so ambiguity is graded per space instead of a binary MAYBE. The split view already renders it when present. | Dean's question: five fresh sensors at 830° should read as near-certain, not coin-flip. The allocator needs P(burning) anyway. |
| Sat hour 10 | Graded belief landed (CP2 prompt 4, last estimator change before the freeze): `Belief.probability` per space = weighted fraction of kept hypotheses (weight `exp(-(score-best)/tolerance)`), times the 0.8 suspect penalty only for the space's own distrusted sensor; Kalman reports 0 or its scalar confidence. Forced candidates: a space whose TRUSTED reading has been above ignition (250 C) for 3 consecutive ticks goes into every candidate, gated by an assumed fuel budget (1/0.02 ticks, 1.5x faster for 'fuel' hazard, minus a 5-tick margin) counted from when the brain first believed the space burning. Also fixed: the impossible-rise/drop bounds now let a neighbor with no trusted reading be anywhere between ambient and flame temperature. New metrics: Brier, per-space FPR/FNR at P>=0.5. | Before, seed-42 freeze tick 50: ours [S1,S2] at 0.16 with S3..S6 one flat MAYBE while F4,F5,F6 read 800+ honestly. After: [S1,S2,S4,S5,S6] at 0.40, S3 MAYBE at P~0.4. Evidence (5 seeds, 60 ticks): false certainty ours 0% on all three modes both before and after; coverage 91% -> 93%; mean error 3.6/3.7/8.0 -> 4.6/4.8/10.2 C (freeze/blind/flashover); Brier ours 0.067/0.069/0.078 vs Kalman 0.123/0.181/0.117. Why the fuel gate: without it a burned-out space (still 750 C from its burning neighbors) is forced forever, and on a 120-tick run every space ends up forced into one hypothesis, i.e. false certainty after total burnout; with it, scoring takes over and a burned-out space becomes a MAYBE or is dropped. Why the bound fix: the world-consistent test streams showed an honest sensor flagged impossible-rise when its space ignited next to a space whose sensor had died (estimate 429 C vs true 512 C). Tried and reverted: rolling a hypothesis with each space burning only since the brain first believed it (fixes the parent fire dropping to MAYBE for ~3 ticks when a neighbor ignites, but removes the leakage signal that separates "just ignited" from "not burning", and a clean run then never earns confidence). That MAYBE window is a recorded limitation, not a bug. |
| Sat hour 11 | Split view v2 (CP2 prompt 5): geometry is computed once in `buildViewerData` (`src/eval/layout.ts`) and shipped as JSON in `ViewerData.layout`, so `src/ui/split/svg.ts` and the plain-JS `viewer.template.html` draw from identical positions instead of two copies of the algorithm. Demo beats live in the store as pure `beatConfig` data. `src/shared/structures.ts` on dean-branch registers only demo-6; Chase's version on main registers all three plans with the same exports. Recording mode is `?demo=1` (plus `?plan=`, `?beat=`, `?t=` for filming). | The exporter is the filming backup and must look identical to the live page; a duplicated layout algorithm would drift. At merge, take Chase's `structures.ts`; the split view, exporter and tests only use `PLAN_NAMES`, `loadPlan`, `isPlanName`. `store.ts` keeps Chase's names (`planName`, `setPlanName`, `sanitizeCorruption`, `sanitizeIgnition`, `trace`, `tick`) so his 3D scene mounts without changes; take Dean's store at merge, it is a superset. |
| Sun hour 12 | `computeMetrics(trace, { onset, confidenceThreshold? })` (CP3 prompt 1): every windowed metric, including estimation error, now counts ticks >= onset; `TickRecord` carries `stepMs` (performance.now around `brain.step` only) and `onset` (null for a clean run). `timeToRecovery` = ticks from onset to the first of five consecutive exact matches; `wrongDispatch` = fraction of ticks where the burning set names a space that is neither burning nor hedged as ambiguous. | The brief's four metrics need a defined window and a compute cost per tick. Windowing the error moves the CP2 evidence mean error from 4.6/4.8/10.2 to 4.9/5.1/10.9 C (freeze/blind/flashover) and Kalman's from 42.0/114.7/41.9 to 44.8/122.7/44.7; false certainty, coverage and Brier are unchanged. `stepMs` is wall-clock, so determinism checks compare traces with it stripped. |
| Sun hour 15 | The method freeze (CP3 prompt 3) is deferred until Sunday evening ET, the real hour 36. CP3 prompts 1 and 2 (metrics, sweep) are done; prompt 3 is deliberately not run. `docs/06-freeze.md`, `docs/07-what-surprised-us.md` and `src/eval/freeze.test.ts` do not exist yet, so the freeze hook is not active and `src/brain` and `src/corruption` are still open. Dean reviews the sweep's WHERE OURS LOSES block Sunday morning and decides whether one more estimator change is worth it before freezing. When the freeze runs, `ci.yml` needs `actions/checkout` with `fetch-depth: 0` so the freeze test can diff against the recorded hash. | Freezing 20 hours early throws away the option to fix what the sweep exposes, and an unfreeze-refreeze makes the first freeze's numbers worthless. One real freeze at the scheduled hour is the clean story for judges. |
| Sun hour 15 | `dean-branch` stays unmerged until CP2 and CP3 are both fully done; Dean merges to `main` himself then. Chase keeps pushing `chase-branch` after each checkpoint and rebases on `main` when Dean merges. Until then, Chase's agents should read `origin/dean-branch` for the graded belief, split view v2, four metrics and sweep, and treat `src/shared/structures.ts` on `chase-branch` as the one to keep at merge time. | Dean's call, replacing the merge-at-every-checkpoint rule from Sat hour 3. Dean's work on CP2 and CP3 is one continuous change to the estimator, harness and split view; merging it in halves would put a half-verified estimator on `main`. |
| Sun hour 16 | Merged `origin/main` (Chase's CP2 world, CP3 store plumbing, chaos panel, three-column scene page) into `dean-branch`. Conflict resolution: `src/ui/store.ts` is Chase's config-driven store (brains toggle, per-brain traces, `usePlayback`, versioned persistence key `firefly.sim.v2`) with Dean's demo beats, recording mode, URL presets (`fromUrl`), `validateRun`, `hottestNeighbor` and `beatConfig` added on top; every manual control change clears the last beat's caption. `SplitView.tsx` is Dean's v2 minus its own playback loop and its own plan select, both of which App now owns. `store.test.ts` is the union of both test files. `src/shared/structures.ts` is Chase's (all three plans). `package.json` keeps both `sweep` and `gen:plans`. | Chase's panels, scene view and playback hook all import the store's new shape, so his version had to be the base; Dean's additions are additive. One plan select and one playback loop per page. |
| Sun hour 17 | Two pre-freeze estimator fixes, both Dean's call after the CP3 sweep. (1) Total blackout carries the last known belief forward: with zero trusted readings the brain now reports its previous burning set and probabilities at floor confidence 0.05, with the estimate advanced one tick by physics, instead of letting the no-liars tie-break report "no fire". (2) A space's P(burning) is capped at 0.9 until it has been in every surviving hypothesis for 2 consecutive ticks; before, a forced hot space could drag a wrong neighbour into every survivor for one tick and print P=1.00 with scalar confidence 0.36 (seen in blind mode). Evidence table unchanged to two decimals; full three-plan sweep still has no cell where ours loses. Burned-out-but-hot spaces (the whole 14–25% per-space FP) are deliberately left alone: burned-out is inferred from an assumed fuel load, not observed, and a 700 °C room still needs cooling before entry, so the fix belongs in the metric (split FP into "hot, no longer generating" vs "cold and wrong"), which is eval code and stays open after the freeze. | A commander with no data wants "last known fire, unconfirmed", not a clean sheet. One tick of unanimity is not certainty, and per-space P=1.00 while wrong is exactly the false certainty the thesis says never happens. |
| Sun hour 18 | Five review findings from an outside read of the CP2 report, all accepted and acted on before the freeze. (1) **Gated Kalman baseline** (`createGatedKalmanBrain`): chi-squared innovation gate at 9 (one degree of freedom), a sensor rejected 3 ticks in a row is re-accepted so a real ignition is not rejected forever; rejected sensors are reported as suspect. Evidence, sweep and the mismatch probe now report naive KF, gated KF, ours. (2) **Kalman probabilities are the filter's own posterior**, Phi((x_i − 200)/sqrt(P_ii)), not the hand-written 0-or-confidence label; Brier is now scored against what the filter believes. (3) **Kalman process model corrected to the world's**: it had kept the constant +40 C/tick source and no ambient cooling from the CP1 world, which the world replaced at hour 5 with the pull-to-flame model; a baseline with stale physics is a straw man. Its temperature error under flashover fell from 44.7 to 12.9 C after the fix. (4) **False certainty on probability**: `Metrics.falseCertaintyByP` at thresholds 0.5/0.7/0.8/0.9/0.95 = fraction of ticks where a not-burning space was given P ≥ threshold; printed as a curve by `npm run evidence` and as `fc@P.9` in the sweep. (5) **Model-mismatch probe** `npm run probe:mismatch` (brains see edge rates × U(1−s, 1+s), world keeps the truth) and `docs/06-freeze-plan.md` written before the freeze: scope of the frozen object (Observation → Belief, not the closed-loop trajectory once the allocator ships) and six held-out families with fresh seeds, committed unrun. | Findings from running them, hour 18, demo-6 unless stated: **(a) The Kalman's 65 % "confidently wrong" is a clean-run number.** With no corruption at all it is 65 % on demo-6 and 88 % on vessel-3x8, exactly the fraction of ticks where some space is above 200 C but not burning (burned out, or heated by a neighbour). The baseline equates hot with burning; corruption barely moves that number (freeze 65 %, blind 72 %). So the false-certainty headline measures hot-vs-burning separation, which ours does and Kalman cannot by construction; **corruption resistance is measured by temperature error and coverage**: Kalman 2.2 C clean → 42 C freeze → 123 C blind, ours 0.4 → 5 → 5; Kalman coverage 33 % under blind. The evidence table now prints the clean row first so this is visible. **(b) Gating does not rescue the baseline**: gated KF false certainty 62–69 % (vs 64–72 naive), and it is a worse thermometer on a clean run (18.7 C vs 2.2) because the gate cannot tell model error from sensor error and throws out honest readings. It catches a blinded sensor at once, catches a frozen sensor only intermittently, and never doubts the fire the stale value describes. The reviewer's guess (gating closes the freeze gap) was wrong; recorded as such. **(c) Ours on the probability curve**: 0–1 % at P ≥ 0.9, 3 % at 0.8, 16–28 % at 0.5; the 0.5-level mass is burned-out-but-hot spaces held at 0.5–0.8. Not zero at every threshold; reported as is. **(d) Mismatch ±30 %** (seed 1, demo-6 and vessel-3x8, none/freeze/blind/flashover): ours' false certainty stays 0 %, coverage and temperature error unchanged to 0.1 C, wrong dispatch +1 point. Not brittle at ±30 %; ±50 % is a post-freeze family. |
| Sun hour 21 | **Source-estimating Kalman baseline** (`createSourceKalmanBrain`, `kalman-source` in evidence, sweep, mismatch): an augmented-state filter over [x, q] where q_i is the heat added per tick by a source in space i (random walk, Q_q = 4, P0 = 100; A0 has NO generation term). A space is burning iff q_i / sd_i > 3 and q_i > 10 C/tick; P(burning) = Phi((q_i − 10) / sd_i). No gating, no fuel guess, no fire logic: the textbook fix for "hot is not burning". It is the fair baseline because the naive filter's 65 % clean-run false certainty was our chosen label rule (x > 200 C) being wrong on burned-out and neighbour-heated spaces, not the filter being fooled. WHERE OURS LOSES now compares against the best of naive, gated and source. | Clean-run (mode none, 5 seeds, 60 ticks, scored from tick 0; `npm run evidence` windows from onset 5 and prints 65 % → 14 % for demo-6) false certainty before → after: demo-6 61 % (naive) → 13 % (source); tower-5x4 69 % → 0 %; vessel-3x8 88 % → 78 %. The vessel number stays high partly because two spaces (L2-B2, L2-B3) have no sensor (about half of the confidently-wrong ticks on seed 1 involve them): the filter cannot place a source it cannot observe, so it misses one space of a 24-space set for 30+ ticks while its temperature confidence stays at 0.93; a one-space miss on a big set is "wrong set at high confidence" by the metric's definition. On demo-6 the source filter is also the best-calibrated brain on a clean run (Brier 0.027 vs ours 0.043) and is a better thermometer (0.9 C vs 2.2). Under corruption it is as fooled as the naive filter: freeze 59 %, blind 73 %; under flashover its false certainty is 2 % only because with no readings its covariance grows and confidence falls, while coverage drops to 24 % and error to 152 C (it loses the fire rather than lying about it). Ours: 0 % on every mode, coverage 93 %. Quick sweep (demo-6, vessel-3x8, tower-5x4, 2 seeds, 60 ticks): no cell where ours has higher false certainty or wrong dispatch than any of the three baselines. Estimator untouched. |
| Sun hour 22 | **Moving sensors and the command hook, the last two estimator changes before the freeze (CP3 prompt 2c).** (A) `consistency.ts`: the rate-of-change rules already judged a reading against `prevEstimate[spaceId]`, never against the sensor's own last value, so a drone flying from a cold room into a hot one was never an "impossible rise" on that account; what was wrong was the frozen rule, whose window now only counts history entries taken in the space the sensor currently reports from (a flight is not a temperature change, and a drone that moved inside the 6-tick window cannot be frozen; one parked for the whole window can). A FIXED sensor whose spaceId differs from its previous entry is judged against its own last reading, because a fixed sensor cannot move. Two trusted readings in one space (fixed + drone) average in the estimate, as before; confirmed by test. **New, found by probing the prompt's own test scenario:** a SENSOR's first reading from a space (no previous entry, or a drone whose previous entry was elsewhere) is a *first observation* and is exempt from the rise/drop bounds; keyed on the sensor, not the space, so a stationary sensor that misses a tick and returns lying is still held to physics (the verifier showed the space-keyed version let a gap-then-lie print P=1.00 on a cold room). Without it the CP4 scout's reading from an unsensed room was flagged impossible-rise against the physics rollout of whichever hypothesis happened to score best, and stayed suspect forever, so the two-way ambiguity it was sent to resolve never resolved. The same allowance already applied to unsensed neighbours and to every space in the first three ticks. Stale, frozen, cold-in-hot-neighbourhood and no-heat-path still apply to a first observation. (B) `src/brain/commands.ts` exports `planCommands({ plan, belief, kept, drones, prev })`, returning `[]` today; `index.ts` calls it at both return points (normal and blackout) with the kept hypothesis sets (the carried-forward set under blackout) and last tick's commands, and `reset()` clears them. That was the last edit to `index.ts` before the freeze; the CP4 allocator changes `commands.ts` and adds `allocator.ts` only. `docs/06-freeze-plan.md` section 1 and CP4 prompt 1 updated accordingly. | `npm run evidence`: all 16 rows unchanged to two decimals (no drone moves in open-loop runs; no sensor comes back after a gap in these modes). Quick three-plan sweep: every row identical to the pre-change run, WHERE OURS LOSES still empty. Brain test: on a five-space line with one fixed sensor in the middle, the {S2}|{S4} ambiguity resolves to S2 the tick the drone reading from S2 arrives. |
| Sun hour 23 | **The allocator (CP4 prompt 1)** lives in `src/brain/allocator.ts`, called from the `planCommands` hook; no frozen file changed. Scoring is the prompt's: containment by class (tether P(burning)·(1+0.5·occupants+hazard), retardant P(front)·fuel·(1+hazard), hatch P(burning)·open doors to unburned), information = expected shrink of the kept hypothesis set from a trusted reading (steady-state buckets at 200 C, zero where a fixed sensor or drone already reads), score = containment + w_i·information − 0.05·hops, greedy with diversity (×0.3 info, ×0.6 containment once a space is taken), 15 % hysteresis, retardant refill below 0.15, safety (free-flyers never above a 400 C estimate). Three deliberate deviations from the prompt, all in the safety and information rules: (1) a space in `burningSet` is unsafe for a free-flyer whatever its estimate says, because the estimate of a believed-burning space is usually a rollout (its sensor is the one that died) and lags the truth; (2) the tether cap is 800 C, not the prompt's 600: tethers die at 900 (changed at Sat hour 6, after the prompt's 500-based figure) and a burning space plateaus near 770, so at 600 a tether could never engage an established fire and, once inside one, was told to hold and stopped suppressing; safety governs sending a drone somewhere, and a drone may act where it already stands; (3) a drone's own reading does not count against its own space for information, so a scout on an informative space stays instead of leaving and coming back every tick (the verifier found the ping-pong). **Closed loop is opt-in at the loop:** `LoopConfig.dispatch` (additive contract field, default false) decides whether the world applies the primary brain's commands; an open-loop trace records no commands. `LoopConfig.drones` passes a roster through to the world. | Two reasons for the flag. (1) The freeze plan says every estimator number is reported on the open-loop family; with the hook now live, `createBrain` dispatches on every run, and the evidence table, sweep, target resolution (`hottestNeighborAt`), the graded-belief test and Chase's store, scene and demo-beat tests all changed or broke (on demo-6 two tethers hold the fire to two spaces; on vessel-3x8 and tower-5x4 it is out by tick 28–29 with one space ever burned, which erases the divergence the chaos-panel beats show). Default-off keeps every existing run, including tonight's freeze sweep, byte-identical, and the UI unchanged until Chase opts in with `dispatch: true` in the store's `runLoopMulti` call. (2) The hook's signature is fixed by the frozen `index.ts`, so no per-brain knob can reach the allocator; the loop is the one place that already decides whose commands drive the world. Clean closed-loop probe, 3 plans × 3 seeds, 60 ticks: no suspects, zero false-certain ticks, one space ever burns and the fire is out by tick 29 (demo-6, vessel-3x8) or 43 (tower-5x4); open-loop the same runs burn 6, 24 and 20 spaces. Drone deaths every run: the retardant coating the front dies when the front ignites (the prompt's 400 C rule has no margin against a space heating past 400 within the tick), and a hatch on demo-6. **Finding for the writeup:** tethers oscillate on clean closed-loop runs (counting a drone's goTo going A-B-A over three consecutive ticks: the two tethers score 24 and 7 such events in 60 ticks on vessel-3x8, 23 each on tower-5x4, seed 1) because the frozen estimator has no suppression model: a fire two tethers are cooling looks, to the physics, like a space that is not generating heat, so `burningSet` flips between the fire and its neighbours and the tethers follow it; 15 % hysteresis cannot hold a target whose P(burning) drops from 1 to 0.5. Recorded, not tuned. Those are the closed-loop numbers CP4 prompt 3 reports. |
| Sun hour 24 | **Show the hedge (CP4 prompt 2).** `Metrics.hedgeRate` (fraction of ticks ≥ onset where `belief.ambiguous` is non-empty and the tick's commands name two or more different spaces inside ONE ambiguous group; the same rule as Chase's HEDGING badge in `src/ui/scene/hedge.ts`) and `Metrics.containmentDelta` (spaces burning at the last tick with the allocator minus with a null allocator; filled by `runContainment()` in `src/eval/containment.ts`, which runs one seed twice with `dispatch: true`, the second time through `withoutCommands(createBrain)`). `npm run hedge` (`src/eval/hedge.ts`): the prompt's configuration, flashover on the ignition space, k=2, the largest plan (vessel-3x8, 24 spaces), 2 scouts + 2 tethers + 1 retardant via `LoopConfig.drones`, closed loop; prints onset..onset+20, the hedged ticks (marking any after the fire is out or outside the window) and the two numbers to `results/hedge-cp4.txt`; any other configuration writes a config-stamped file so the CP4 artifact is never clobbered. | **The prompt's configuration does not hedge while the fire burns**, on any of seeds 1–5: its only hedge (seed 1, tick 36) comes after the fire is out, when both tethers are sent to "suppress" spaces of a belief that names no fire; the artifact says so on its face, and the loop test asserts it as a recorded fact rather than forcing it. The verifier caught an earlier version of this row claiming hedges at ticks 21 and 25; those commands targeted a space outside the ambiguous group and were not hedges. Closest configurations at the time (same roster, k=2, 60 ticks, seeds 1–5, hedges while burning and inside the window): vessel-3x8 blind hedged at tick 7 on every seed; vessel-3x8 mixed seed 1 on 8 window ticks; tower-5x4 mixed seed 3 on 16. **Superseded at CP3 prompt 2d** (the suppression-aware rollout removed most belief flips, and with them most hedges): the prompt's configuration now never hedges at all, blind never hedges, and the closest run is vessel-3x8 **mixed seed 2**, hedging at ticks 12–15 while L1-B3 burns (committed as `results/hedge-cp4-vessel-3x8-mixed-2.txt`, the video's second artifact); the only other in-window hedges on seeds 1–5 are demo-6 mixed (seeds 2, 4, one tick each), tower-5x4 freeze seed 4 and mixed seed 4 (one tick each). Why hedging is rare by the strict definition: (a) the estimator groups ambiguous spaces into connected components, so the two sides of the prompt's own example, S2-or-S4, are two singleton groups and covering both is not a hedge by this definition nor by the badge's (a diagnostic line in the report counts commands covering two or more ambiguous spaces across all groups); (b) a scout's informative targets are candidate fires, usually above the 400 C estimate a free-flyer may enter, so scouts mostly observe from a safe neighbour; the hedges that occur split a scout from the tethers (the blind artifact's tick 7: D2 observes L2-B3 while D3 and D4 suppress L1-B3, both inside the group {L1-B2,L1-B3,L1-B4,L2-B3}) or split the tethers across a burning space and its ambiguous neighbour. containmentDelta is large and consistent: −4 on demo-6 (−1 to −2 under mixed, where the open-loop fire is also smaller), −21/−22 on vessel-3x8, −19 on tower-5x4 (0 spaces burning at tick 60 with the allocator versus 4/21/19 without) on every configuration tried: two tethers put out every fire on these plans. In the demo run a scout and the retardant die on the first command tick (the belief names the neighbour, not the fire, at tick 5). |
| Sun hour 25 | **The commander's brief (CP4 prompt 3).** `withAllocator(factory)` in `kalman.ts` wraps any brain so its commands come from the shared `planCommands` hook with `kept = [burningSet]` (one hypothesis: it dispatches, never hedges); `createKalmanDispatching`, `createSourceKalmanDispatching`. `src/eval/outcome.ts` `computeOutcome(trace, plan)`: fireVolume, peakBurning, spacesBurnedOut, containedAt (ever-burned set stops growing ≥ 10 ticks before the end), extinguishedAt, dronesUsed (ignores hold), tetherTicks (tether on suppress in its target space), retardantSpent (sum of resource drops), droneDeaths, wrongFloor (non-hold commands to a space neither burning nor adjacent to one, in truth), hedges, and a tick-ordered events list; exported `BriefFile` type is the JSON the CLI writes and Chase's CP5 panel should render. `src/eval/brief.ts` `renderBrief()`: Situation / Play by play (≤ 40 lines) / Outcome / one closing sentence, templated, byte-deterministic, no API call. `npm run brief -- --plan --mode --seed --ticks [--brains]` runs each brain closed-loop on one seed (sweep corruption defaults: onset 5, k 1, target = ignition) and writes `results/brief-<plan>-<mode>-<seed>.md/.json`; `npm run brief:sweep` runs task D and writes `results/brief-sweep.md/.json`. | **Task D means (demo-6, vessel-3x8, tower-5x4 × freeze, blind, flashover × seeds 7, 8, 9; 120 ticks; same allocator), ours / naive Kalman / source Kalman:** fireVolume 30.7 / 38.0 / 38.3 space-ticks; extinguishedAt 31.7 / 39.0 / 39.3; spacesBurnedOut 1 / 1 / 1; tetherTicks 22.9 / 47.2 / 99.0; retardantSpent 2.0 / 3.3 / 6.0; droneDeaths 1.2 / 0.1 / 0 per run; wrongFloor 27.1 / 10.3 / 74.8 commands; hedges 4.4 / 0 / 0. Per plan, ours extinguishes sooner on every plan (demo-6 29 vs 34/34, vessel 29 vs 32.9/34, tower 37.1 vs 50/50) and per mode too. **The claim holds on "sooner" and "less water" everywhere; "fewer spaces burned" is a tie, because two tethers reach the fire before it spreads under every belief, so one space burns in all 81 runs; "fewer drones to the wrong place" is FALSE against the naive Kalman on every plan and mode** (ours 13.1 vs 11.3 on demo-6, 45.4 vs 10.7 on vessel-3x8, 22.9 vs 9 on tower-5x4) and ours loses about one drone per run where the naive filter loses almost none. Why: the naive filter's belief is a stable single set, so its tethers park on the fire and its scouts idle; ours carries ambiguous neighbours, sends scouts and the retardant toward them (a hedge to a space not adjacent to the true fire counts as wrong floor by this metric, deliberately), and its burning set flips as the suppressed fire cools (see Sun hour 23), so tethers retarget and a scout or retardant standing next to the fire when the belief moves dies when the space reheats. The source Kalman is the worst dispatcher (99 tether-ticks, 75 wrong-floor commands): its source estimate keeps naming already-cooled spaces, so its tethers chase ghosts. **Updated at CP3 prompt 2d (suppression-aware rollout), same grid, ours / naive Kalman / source Kalman, before → after:** extinguishedAt 31.7 / 39.0 / 39.3 → 39.3 / 39.0 / 39.3; tetherTicks 22.9 / 47.2 / 99.0 → 77.8 / 47.2 / 99.0; retardantSpent 2.0 / 3.3 / 6.0 → 4.6 / 3.3 / 6.0; droneDeaths 1.2 / 0.1 / 0 → 0 / 0.1 / 0; wrongFloor 27.1 / 10.3 / 74.8 → 32.4 / 10.3 / 74.8; hedges 4.4 → 0.0. The baselines are unchanged (they never modelled suppression). Ours' "sooner and less water" before the fix was an artefact of the defect: the tethers abandoned the fire when it cooled, it reheated, and the crash-cooling on their return pushed it under the world's ignition floor at tick 29; with the tethers held on the fire it plateaus near 280 C and burns until its fuel is gone at tick 34, exactly as under the baselines' beliefs. Drone deaths went to zero. wrongFloor did NOT improve (32.4 vs 27.1): with the belief now stable, the retardant, hatch and scouts spend the whole run on coat, close-door and observe commands to spaces one or two doors from the fire, and after the fire is out every such command is "wrong" by the metric; the prompt says to stop there, so nothing else was tuned. Two readings of the numbers a judge should have: wrongFloor counts every non-hold command to a space neither burning nor adjacent to one, so after the fire is out every command is "wrong" by definition (of ours' 27.1, 14.5 are post-extinguishment and 2.5 are refills; the naive Kalman's 10.3 is 5.4 refills and 3.9 post-extinguishment); restricted to non-refill commands while the fire burns it is 10.1 / 1.0 / 3.3 and ours still loses. containedAt is 1 in all 81 rows because no fire ever spreads under any belief, so it is not a discriminating result here. Not tuned in this prompt; the estimator is frozen and the allocator weights are the prompt's. |
| Sun hour 26 | **The estimator learns that water exists (CP3 prompt 2d), the last estimator change before the freeze.** `physics.ts`: `TETHER_SUPPRESSION = 0.3` (mirror of the world's) and `forward(plan, temps, burning, suppression?)`: a burning space with n tethers (capped at MAX_TETHERS) generates 0.3ⁿ of its heat and every space with tethers loses n × 0.1 × (T − ambient) more per tick, the world's exact effect of a tether on 'suppress'; no map = the old behaviour. `hypotheses.ts`: `predict()` and `score()` take the same map and pass it to every rollout step (tether placement held constant over the 3-tick window). `index.ts`: `tethersAt` is built each tick and passed to every `score()`, the one-step `predict()` for unsensed spaces and the blackout carry-forward. **Two deviations from the prompt's letter, both forced by the byte-identical-evidence requirement.** (1) The prompt said to count OBSERVED alive tethers per space. Its premise, that open-loop runs have no drones, is false: the world's default roster parks two idle tethers at the resupply space on every run, and the first implementation changed the evidence table (when the fire reaches the resupply space the brain predicted water that never flows). A tether now counts only where the brain's own last command told it to suppress AND it reports standing there, the world's rule exactly; commands are the brain's deterministic function of past observations, so the input is still the observation history. (2) That alone was not enough: in an open-loop run the harness discards the commands but the brain still issues them, so once the fire reached the parked tethers it again assumed obedience. `BrainConfig.dispatch?: boolean` (additive contract field; the loop passes `LoopConfig.dispatch`, and only the primary brain of `runLoopMulti` gets true) tells the brain whether its commands are applied; when false no tether is ever working. The open-loop family the freeze record is measured on is now open-loop through and through. | Flip counts (a space leaving burningSet and re-entering; demo-6 / vessel-3x8 / tower-5x4, seed 1, 60 ticks, dispatch on, mode none, two-tether roster): **14 / 27 / 36 → 0 / 0 / 0**; with the default roster 8 / 11 / 24 → 0 / 0 / 0; tether A-B-A retargets on vessel-3x8 24 and 7 → none, on tower-5x4 23 and 23 → none. `npm run evidence` byte-identical to `results/evidence-cp2.json`; 60 open-loop traces identical. Regression test (test-3 plan, S1 burning, two tethers visible from tick 16, S1 falling 774 → 279 C while burning): S1 stays in burningSet on every tick 16–40 at confidence 1.00; the control without visible tethers also keeps S1 (its reading never drops below ignition, so the forced rule pins it) but at confidence 0.28, the misfit the fix removes. Closed-loop means before → after are in the CP4 prompt 3 row above: deaths 1.2 → 0, extinguishedAt 31.7 → 39.3 (now equal to the baselines; the earlier "sooner" was the defect's artefact), tetherTicks 22.9 → 77.8, wrongFloor 27.1 → 32.4 (not improved; stopped there as instructed). Side effect: hedges are now rare (the flips were producing most of them); the CP4 prompt 2 pins and artifacts were updated to the new closest configuration. |
| Sun hour 27 | **The identifiability result (CP5 prompt 1).** Plans `data/ident/ident-7.json` and `ident-7-fixed.json` (not in `data/structures`, which is Chase's; imported directly by `src/eval/identifiability.ts`, not registered in the plan registry). Construction departs from the prompt's sketch: sensors sit only on spaces fixed by the wing-swapping symmetry (P, Q, R), the wing interiors A1–A2 / B1–B2 are unsensored and A2/B2 carry no fuel. The prompt's version, with sensors at the wing ends A2 and B2, cannot satisfy its own test (a sensor one door from a wing fire differs by hundreds of degrees between H1 and H2, never within 2 σ), so the base plan is exactly symmetric (max difference 0.0 C) and the k=1 question is answered on the resolved plan, where the added sensor FA1 is the one a freeze or blinding must hit. `npm run ident` writes `results/identifiability.txt`; `docs/05-identifiability.md` is the page. Step 4 runs closed loop with two scouts and a tether. | Headline numbers: base plan, every sensor identical under H1 and H2 (0.0 C; 2 σ = 4 C); A1 itself differs by 548 C. Open loop: ours reports {A1,A2} | {B1,B2} ambiguous at confidence 0.14, coverage 93 %, false certainty 0 %; naive Kalman names neither wing at 0.84–0.87 (coverage 18 %, wrong dispatch 47 %), source Kalman the same (15 %, 57 %). Closed loop: a scout's cold reading from B1 resolves the pair at tick 5 by elimination, at 104 drone-ticks parked in the empty wing versus 0 on the fixed plan; containment identical (one space ever burns). Fixed plan: ours names A1 from tick 1; naive Kalman names A1 and the empty void A2 at 0.90 (false certainty 13 %). Blinding FA1 puts the B wing back in play on 37 of 40 ticks. Caveat recorded in the report: the passage reaches the world's spontaneous-ignition band around tick 40 and the fire reaches the B wing by tick 60, so the pair is genuinely ambiguous only before then; containment is read at 30 and 60. |
| Sun hour 28 | **The method freeze (CP3 prompt 3).** Freeze commit `457de46847453854b93bdbaa807c0a17a2e844d4`, HEAD after committing the full three-plan sweep (`npm run sweep -- --plans demo-6,vessel-3x8,tower-5x4`, 7200 rows, four brains, 367.5 s: over the prompt's 5-minute budget because the table has four brains, not two; seeds not reduced). `docs/06-freeze.md` copies sections 1 and 2 of the freeze plan verbatim, adds the closed-loop sentence on the frozen object's inputs, the hash, the `CorruptionConfig` type and modes, the estimator paragraph, the sweep table verbatim and the WHERE OURS LOSES block with a diagnosis. `src/eval/freeze.test.ts` diffs the frozen files against the hash (skipped when the doc does not exist); `.github/workflows/ci.yml` checks out with `fetch-depth: 0` so CI can run it. `docs/07-what-surprised-us.md` is the checkpoint-3 video script. README updated. | Nine losing cells, all saturate with a random target, ours' wrong dispatch 41–49 % against the source filter's 7–23 %, at 0 % false certainty and 87–89 % coverage against 45–55 %: every hot sensor pins at 300 C, the readings stop carrying information, and the forced-space rule keeps burned-out spaces until the assumed fuel budget runs out. No cell where ours has higher false certainty than any baseline. Nothing in `src/brain` or `src/corruption` changed in this prompt. |
| Sun hour 27 | **Head to head is closed loop, and each side commands (CP5 prompt 1, after the allocator merge).** `store.runCompare()` runs the scenario twice with `dispatch: true`, ours driving (that run becomes the main traces) then Kalman driving, both through `H2H_FACTORIES = { ours: createBrain, kalman: createKalmanDispatching }`, i.e. Dean's `withAllocator` wrapper, so the containment curves compare two beliefs commanding the same drones, not a commander against a spectator. A `dispatch` toggle ("drones act on commands", persisted, default OFF) controls the split, scene and compare views; off is the open-loop family the freeze record is measured on, and the demo beats stay byte-identical. `runShowdown()` (button "Showdown: blind from t=1") sets blind, k=1, onset 1 on the ignition sensor and runs head to head. A MISSED tag names a burning space a brain neither claims nor hedges on (the complement of WRONG FLOOR). | Probed 3 plans × freeze/blind/flashover, closed loop: **with the usual onset 5 both worlds contain identically** (peak 1, out at t34 on demo-6 and vessel-3x8), because both brains lock onto the ignition space before its sensor dies and two tethers put out a one-space fire under any belief; the estimator's edge shows only in estimation error (0.45 vs 5.88 on demo-6 freeze) and the scorecard. With the ignition sensor blind from t=1 the worlds diverge: demo-6 seed 42 ours peak 1 (out t34, 0 drone deaths) vs Kalman-driven peak 6, 3 still burning at t60, 4 dead drones; flashover from t=1 the same (peak 1 vs 6). Freeze from t=1 does not separate them (a frozen ignition sensor reads its cold last value and both brains find the fire from its neighbours). Two-ignition runs separate the curves without an early onset (demo-6 S3+S5 freeze onset 3: sum 182 vs 238 burning space-ticks; tower-5x4 blind k=3: 926 vs 955) but less legibly. The showdown button exists because the default beat config would show two identical curves and a judge would read that as "no difference". Dispatch defaults off so `npm run evidence` and every open-loop test stay unchanged; the head-to-head view ignores the toggle (a head to head with nobody in command is not one). |
| Sun hour 28 | **Demo-proof (CP5 prompt 2).** The beats are now the pitch script on keys 1-5: clean run (opens on `SCRIPT_PLANS.first`, vessel-3x8), freeze the ignition sensor, flashover, head to head (same config, closed loop, switches the page to Head to head), different building (`SCRIPT_PLANS.last`, tower-5x4, same config). The CP3 `blind` beat stays as key 6 and `?beat=blind`. The page (`view`) is store state so a beat and the `c` key can switch it; `r` toggles recording mode; keys work on every page (one handler in App). Recording mode on every view hides the tuning knobs and the debug tables and keeps the script, the chaos presets, the scene, the briefing and the scorecard. Briefing panel under the belief view: the belief's own `briefing` string if a brain ever supplies one, else the latest brain-side event from Dean's outcome model (`sensor-suspect`, `dispatch`, `hedge`; truth-derived events such as ignitions are excluded because a brain could not know them), CSS-only typewriter. Error boundaries around each page and each 3D canvas. No network: `src/ui/no-network.test.ts` fails on fetch/XHR/WebSocket/EventSource/sendBeacon, a dynamic import of a URL, or an http(s) href/src/@import in src/ui, index.html, src/main.tsx and src/index.css; the Google Fonts link in index.html was the one real call and is gone (system font stacks). Stage backup: `npm run export:trace` runs the six beats exactly as the buttons do and writes `results/demo-trace.json` (6.5 MB, numbers rounded to 3 decimals, stepMs zeroed); "load trace" in the top bar reads that file locally and from then on each beat replays its recording instead of simulating (manual Run still simulates; "replay on · 6 beats" shows, click to go back live). `npm run build` + `npm run preview` verified headless with zero external requests. | Why the beat list changed: the prompt's five-step script is the live-demo control surface and its order (compare as step 4, the high-rise as step 5) is what the judges see; the old `blind` beat is still one key away for the recorded CP2/CP3 videos. The head-to-head beat's caption is completed after the run from what the curves show ("Peak burning: ours N, the other world M" or "Both worlds contain it this time ... Showdown is where a wrong belief costs the world"), because with onset 5 both closed-loop worlds usually contain (Sun hour 27) and a caption promising a gap would be false on stage. Why replay is per beat rather than one big trace: the stage story is "press the next key"; if the sim throws mid-pitch, the same key shows the same recorded beat and nothing on screen changes shape. Why the briefing falls back to Dean's outcome events: the contract has no per-step briefing string (Belief carries none; request to Dean: `Belief.briefing?: string`, additive), and an empty panel in every demo would be a dead feature. |
| Mon hour 40 | **Generality (CP5 prompt 3).** `docs/09-generality.md`. The tower runs on the frozen brain unchanged: `npx tsx src/loop.ts --plan tower-5x4 --ticks 120 --mode flashover --k 2` and the quick sweep on tower-5x4 alone (`results/sweep-tower-quick.txt`, WHERE OURS LOSES empty). The scorecard now carries a second table, the plan's sweep row: `npm run gen:sweep-summary` folds `results/sweep-latest.json` (7200 rows) into `src/ui/sweepSummary.json` (3 KB, per plan × brain means of the four scorecard metrics, committed), so the head to head shows the live run and the whole sweep for the same building without bundling the sweep file. The shaft is now the brightest line in the 3D scene (`#67e8f9`, width 5, drawn over the boxes it joins with depth test off, because a line between box centres is otherwise hidden inside the boxes; floors stay `#60a5fa` width 2) as it already was in the split view. The level slicer default is a pure helper (`src/ui/levels.ts`: the top level, which always contains the ignition level; reset on plan change). Beat 5's caption leads with the exact line "Same system. Different structure. Nothing retrained." | Two things learned running the prompt's own command. (1) `npm run sweep -- --plans tower-5x4 --quick` overwrites `results/sweep-latest.json` with the 480 tower-only rows; the doc says to `git checkout` it afterwards, and the summary JSON is generated from the full file on purpose so a quick run cannot silently shrink the scorecard's denominator. (2) The default slicer already showed the ignition level (it shows every level); the prompt's "set the default so the ignition level is visible" is now a tested invariant rather than a new value, because hiding the top two floors by default would undercut the point of the beat (five levels on screen). On the tower the fire climbs the shaft column (L1-A1 to L5-A1 in the run above) exactly as the 0.35 rate says it should, and the unsensed L4 is carried as MAYBE by ours and as burning-at-100 % by Kalman. |
| Mon hour 46 | **Incident replay (docs/10-incident-replay.md).** A documented real fire (One Meridian Plaza, 1991, USFA-TR-049) as a plan file (`highrise-12x9`, floors 20–31 × 9 zones, generated from `plan.spec.json` by the generic `src/world/gen-highrise.ts`), calibrated at **4 minutes per tick** by grid search over the spec's rates against the report's early milestones (`src/incident/calibration.ts`, pinned by test); a **1991 commander baseline** brain that believes exactly what the record says the command post was told, minute by minute, and reads no sensor (`src/incident/commander.ts`); `npm run incident` writing row A (when each floor was known: sim truth, record, ours, Kalman, commander; as-built and instrumented sensors) and row B (closed loop under each belief vs uncontrolled vs the record); a Case file page and beat 7. Incident plans live in a separate `INCIDENT_PLANS` registry (loadable, in the picker, not in `PLAN_NAMES`). `src/incident/` is a new shared directory. | Tick = 4 min because fuel is 0..1 by contract and burns 50 ticks; at 1 min/tick a floor would burn 50 minutes. Symmetric heat transfer is the model's biggest residual (it spreads down to 21; the real fire only dropped there via an open stair); floor 20 gets fuel below the ignition minimum as the plan's bottom edge. Calibration targets only the milestones before firefighting had water (the real fire was fought from ~352 min), so the uncontrolled run (out at 596) is rightly faster than history (1118). **Result kept as found:** every estimator with sensors knows the fire floor at minute 4, before the first engine (8); ours never reaches 0.9 confidence on this fire and holds a wide MAYBE set (0 % false certainty, 64 % coverage) while the Kalman sits at 0.9 and diverges numerically on 108 spaces (Dean's baseline; reported, not fixed). Closed loop, **ours-driven drones do no better than nobody** (9736 vs 9824 space-minutes) because the burning set flips between the unsensed origin zone, its neighbour and the core lobby each tick and the allocator re-targets both tethers every tick, so they never hold; the Kalman's stable over-confident belief parks the tethers on the two burning zones at tick 5 and the fire never leaves floor 22 (396 space-minutes). Same allocator, same drones: target stability, not accuracy, decided it. Recommendation for the allocator (post-freeze, Dean): hysteresis on tether targets under low confidence. Not tuned away after seeing the answer. Not done: a second incident; a directional (buoyancy) vertical edge, which would need a contract change. **Contract file touched (report to Dean):** `src/loop.ts` CLI now validates `--plan` against `ALL_PLAN_NAMES` instead of `PLAN_NAMES`, so `npm run sim -- --plan highrise-12x9` runs; nothing else in the loop changed. The verifier also found the estimators naming the never-burning floors 20 and 31 (hot, not burning); the tables now show every floor so that reads as the false alarm it is. |
| Sun hour 32 | Dean edited Chase's `src/ui/compare/HeadToHead.tsx` and `h2h.css` (ownership waived by Dean for this change, as for the split view at hour 5): when a head-to-head run exists each column renders ITS OWN world (`store.compare[name]`) with that world's drones, wrong-floor and missed tags, instead of both columns showing beliefs on the shared ours-driven world with no drones; the ground-truth canvas grew from 200 to 340 px; the Showdown button has an on-state (`aria-pressed`, amber) and the truth header shows a "SHOWDOWN ON · sensor in <ignition> blind from t=1" tag whenever the corruption on screen is exactly what the button sets. Without a head-to-head run the columns fall back to the previous behaviour. The scorecard is unchanged and still scores both beliefs on the ours-driven world. | The demo is two ships, one per brain, and the drones are the point; the panels were showing one ship and no drones. Chase: pull before touching this file. |
| Sun hour 33 | **Decision log.** `src/eval/narrate.ts` (Dean) turns each tick's belief and commands into commander-voice lines with the reason derived from the belief alone: what it believes (with P and confidence), what it cannot tell apart, which sensors it now distrusts, and every NEW order grouped by target ("L1-B3 on fire (96%): sending 2 water tethers (D3, D4) to L1-B3 to cool it"; "unsure whether L2-A3 is burning or just hot (47%), need a reading there: sending 1 scout (D1) to look"). Standing orders and unchanged beliefs are not repeated. `src/ui/compare/DecisionLog.tsx` renders it live under the head-to-head scorecard, one log per world, up to the current tick with the current tick highlighted (Dean edit in Chase's directory, waived as above). | Dean's ask: the commander needs a transcript of each decision and why, not a table. Narration never reads truth, so it says what the brain thought, and the chart says whether it was right. |
